# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
.idea
# Unedited production query capture used by TestParse_FullProductionCorpus.
#
# Left untracked on purpose: this repository is public and the capture carries
# real article text and real author, publisher and channel IDs. The committed
# stand-in is testdata/queries_anonymized.csv, which keeps every query's
# layout but replaces the contents of its string literals.
testdata/queries.csv